['DEF MAIN:\n', ' t0 = 1 + 2 \n fp[0] = t0', 'END DEF MAIN \n\n']

.section .text
.global MAIN
MAIN:
        sub sp, sp, #4
        mov R0, #1
        str R0, [sp]
        mov R1, #2
        str R1, [sp]
        add R0, R0, R1
        str R0, [sp, #0]

import pickle
infile = open("esamadres", 'rb')
codigoIntermedio = pickle.load(infile)